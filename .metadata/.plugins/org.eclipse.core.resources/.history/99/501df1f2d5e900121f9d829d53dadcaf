#include <windows.h>

LRESULT CALLBACK WndProc(HWND hWnd, UINT msg, WPARAM wp, LPARAM lp);

// WinMain関数
int WINAPI WinMain(HINSTANCE hCurInst, HINSTANCE hPrevInst, LSTR lpsCmdLine, int nCmdShow)
{



	return (int)msg.wParam;
}

LRESULT CALLBACK WndProc(HWND hWnd, UINT msg, WPARAM wp, LPARAM lp)
{
	switch (msg) {

	}

	return 0;
}
